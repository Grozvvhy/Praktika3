<Page x:Class="AgroChem.Technologist.Pages.BatchesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Производственные партии">
    <Grid>
        <DataGrid x:Name="DataGrid" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Рецептура" Binding="{Binding RecipeVersion}" Width="100"/>
                <DataGridTextColumn Header="Техкарта" Binding="{Binding CardVersion}" Width="100"/>
                <DataGridTextColumn Header="Дата старта" Binding="{Binding StartDate}" Width="120"/>
                <DataGridTextColumn Header="Статус" Binding="{Binding Status}" Width="100"/>
                <DataGridTextColumn Header="План. кол-во" Binding="{Binding PlannedQuantity}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>